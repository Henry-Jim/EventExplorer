@page "/"

<PageTitle>Home</PageTitle>

<h1>Hello, world!</h1>

Welcome to your new app.

<button @onclick="test">test</button>

@* @foreach (var item in DataService.GetEvents().Take(10))
{
	<div>
		<h3>@item.Name</h3>
		<p>@item.Description</p>
		<p>@item.Date.ToShortDateString()</p>
	</div>
})
{
	<div>
		<h3>@item.Name</h3>
		<p>@item.Description</p>
		<p>@item.Date.ToShortDateString()</p>
	</div>
}) *@

@code{
	private void test(MouseEventArgs args)
	{
		//var list = get from ticketmaster
		//call datasevice add events methods to add that list 

	}
}